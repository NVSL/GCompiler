#ifndef ${header_name}
#define ${header_name}

% for file in include_files:
#include "../${file}"
% endfor

% for c in components:
% for a in c.args:
% if a[0] is not None:
#define ${a[0]} ${a[1]}
% endif
% endfor
${c.class_name} ${c.var_name}(${",".join([x[0] or x[1] for x in c.args])});

% endfor

#endif